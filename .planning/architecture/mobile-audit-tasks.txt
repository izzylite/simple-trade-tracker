TOTAL 179 | ok=84 minor=60 major=32 bespoke=3

########## BESPOKE ##########

• src/features/calendar/components/TradeGalleryDialog.tsx  [dialog, baseDialog=no]
   - (lines 728-752 (raw <Dialog maxWidth='md' fullWidth, PaperProps height:90vh, minHeight:{xs:'85vh',sm:600})) RAW <Dialog>, not BaseDialog. The dialog already partly thinks about mobile (minHeight xs:'85vh') but is NOT fullScreen, so on a 360px phone it is a centered ~296px card — far too narrow for a trade-detail + side-panel split viewer.
     FIX: Add fullScreen={useIsMobile()} and on mobile set the paper to width/height 100%, m:0, borderRadius:0, plus safe-area insets on the header bar. This is the foundation before the layout fixes below.
   - (lines 755-965 (header bar)) Header is a single non-wrapping flex row containing: prev IconButton + title/counter block + next IconButton + a cluster of up to 4 action IconButtons (Immersive, Game plan, Events, Orion) + Close IconButton. That is up to 7-8 tap targets in one row — guaranteed horizontal overflow at 360px, and the title gets squeezed to nothing.
     FIX: Restructure the header for mobile: keep prev/title/next on row 1 and move the action-button cluster (Immersive/GamePlan/Events/Orion) into an overflow menu (three-dots IconButton -> Menu) or a second wrapping row under sm. Set header flexWrap:'wrap' and reduce gaps. Ensure each IconButton meets 44px tap target.
   - (lines 971-1171 (body flex-row with main column + Orion/Events/NoteViewer side panels as siblings)) The body is flexDirection:'row' with the main trade detail plus OrionPanel/EconomicEventsPanel/NoteViewerPanel rendered as direct flex siblings (desktop split-view). On a 360px phone there is no room for a side panel beside the main column; opening Orion/Events/Note would either crush the main column to zero or overflow.
     FIX: On mobile (useIsMobile()) render the right-slot panels as full-screen overlays/sheets instead of flex siblings — e.g. position:fixed inset:0 (like the existing immersiveMode overlay at lines 1189+) or a bottom Drawer — so they cover the screen rather than sit beside the trade detail. Keep the flex-row split only for sm+. This is dedicated mobile-layout work.
   - (lines 1188-1421 (immersive overlay) and 1204-1293 top bar) The immersive overlay is position:fixed full-screen (good for mobile), but its top bar packs date + name + two Chips ('1/3', 'Trade 1 of N') + Exit button in a space-between row with px:3 — on 360px the two counter chips + Exit + the date/name will overflow.
     FIX: On mobile reduce top-bar px to 1.5, allow the left (date/name) and right (chips+exit) groups to wrap, and consider collapsing the two chips into one ('1/3 · Trade 1/N') or hiding the trade-counter chip. Thumbnail strip (56x40 each, centered, gap 1) is fine but may need horizontal scroll for many images — add overflowX:'auto'.

• src/pages/NotesPage.tsx  [page, baseDialog=na]
   - (lines 281-379 (grid container) + 287-291 (gridTemplateColumns)) At xs the body grid is a SINGLE column (`xs: 'minmax(0,1fr)'`) but THREE children are still placed into it: NoteListPanel, the center editor/viewer, and the right rail (right rail is display:none<lg so it's fine). The list panel and the editor/viewer therefore stack vertically in one column on a phone. NoteListPanel has `height:100%` and the editor also wants `height:100%`, so inside `gridTemplateRows: 'auto 1fr'` they fight for the 1fr row and you get an unusable squashed two-pane stack with no way to toggle between 'browse list' and 'read/edit note'. There is no master/detail switch for phones.
     FIX: Make /notes a master-detail on phone via useIsMobile(): below sm, render EITHER NoteListPanel (when no note selected / not editing) OR the center NoteViewPanel/NoteEditorBody (when a note is selected), with a back affordance to return to the list. Keep the existing `sm:'260px minmax(0,1fr)'` / `lg:` three-col grid for >=sm. Concretely: `const isMobile = useIsMobile();` then `{isMobile ? (selectedNote||isEditing ? <CenterPane withBackButton/> : <NoteListPanel/>) : <existing grid>}`. The back button calls a handler that clears selectedNote / exits edit.
   - (line 31 `const APP_HEADER_HEIGHT = 64;` used at 216 and 230 (`calc(100vh - 64px)`)) Hardcodes 64px header height. On phones the app header is 56px (xs) per styles/layout.ts HEADER_HEIGHT, so the page is 8px too short on xs — and 100vh on mobile Safari includes the URL bar, compounding the offset.
     FIX: Replace the literal with the BELOW_HEADER_HEIGHT token: `height: { xs: \`calc(100dvh - ${HEADER_HEIGHT.xs}px)\`, sm: \`calc(100dvh - ${HEADER_HEIGHT.sm}px)\` }` (import from styles/layout). Use 100dvh not 100vh so the mobile URL bar doesn't push content under the fold.
   - (lines 238-278 sub-header (`px:3, py:1.25`) — creator toggle) Sub-header uses `px:3` (24px each side). With the 360px master-detail fix this only carries a right-aligned My Notes/Orion pill so it fits, but `px:3` is heavy for 360px and the row is justify-content:flex-end only.
     FIX: Reduce to `px:{xs:2,sm:3}` to match PAGE_GUTTER. Pill itself (fontSize 0.74rem, px:1.5) is fine.

• src/features/orion/components/aiChat/AIChatDrawer.tsx  [dialog, baseDialog=no]
   - (BOTTOM_SHEET_HEIGHTS const (line 87-89) + sheet Box sx (lines 333-362)) The chat is a partial-height bottom sheet (fixed height:880px capped at maxHeight:'92vh', borderTopRadius 16) — NOT a full-screen takeover. The task explicitly requires the phone chat to be a full-screen takeover with the input bar sitting above the keyboard. At 360px the sheet width is xs:'100%' (good), but the 92vh cap leaves an 8vh dead strip of backdrop at the top, the rounded top corners read as a half-modal, and when the on-screen keyboard opens it overlaps the AIChatInterface input bar (the sheet height is viewport-relative and does not shrink for the keyboard / visualViewport). This is a raw hand-rolled drawer (Box + fixed positioning), not BaseDialog, so it inherits none of the full-screen/safe-area handling.
     FIX: Gate on useIsMobile(): when mobile, render the sheet as height:'100dvh', maxHeight:'100dvh', top:0, bottom:0, left:0, right:0, width:'100vw', borderRadius:0, transform none (slide from translateY(100%) is fine). Add safe-area padding: paddingTop:'env(safe-area-inset-top)' on the header and paddingBottom:'env(safe-area-inset-bottom)' on the footer wrapper. Use 100dvh (dynamic viewport) so the sheet tracks the keyboard; the inner flex column already pins the input footer to the bottom. Keep the desktop bottom-sheet (sm+) as-is via the breakpoint object.
   - (Header row (lines 371-462)) Header packs avatar (36px) + title/subtitle (flex:1) + OrionUsageRing + Settings IconButton + Close IconButton with px:2.5 gutters and gap:1.5. At 328px content width this is ~5 elements competing; the subtitle truncates (ellipsis, OK) but the two trailing IconButtons + ring eat ~120px leaving the title cramped. Workable but tight.
     FIX: On useIsMobile() reduce px to 2 and gap to 1; the subtitle already has ellipsis so it absorbs the squeeze. Optionally drop the avatar to 30px on xs. Minor — no overflow, just density.
   - (Tab row (lines 466-651)) The segmented tab pill ('Chat' + 'Market Research' with badges) is justify-content:space-between against a 4-icon action cluster (New/Back, History, Reminders, Memory Logs). 'Market Research' is a long label; with the 4 trailing 32px IconButtons the row total can exceed 328px, forcing the action cluster to wrap or the pill to clip. No flexWrap is set so it will overflow horizontally on the narrowest case.
     FIX: Add flexWrap:'wrap' to the tab-row container (line 466 sx) so the action cluster drops to a second line if needed, OR on useIsMobile() shorten the second tab label to 'Research' and reduce action-button gap to 0.25. Wrapping is the safest no-overflow fix.

########## MAJOR ##########

• src/features/calendar/components/import/ImportMappingDialog.tsx  [dialog, baseDialog=yes]
   - (lines 710-722 (slotProps.paper override)) Although built on BaseDialog (which sets fullScreen={useIsMobile()}), this dialog passes its own slotProps={{paper:{sx:{... maxWidth:560, height:'85vh', borderRadius:2, overflow:'hidden'}}}}. Because BaseDialog spreads {...rest} AFTER its own slotProps, this object REPLACES BaseDialog's paper sx — including the full-screen branch ({maxHeight:'100%', borderRadius:0, border:'none'}). The explicit height:'85vh' and maxWidth:560 also win over MUI's fullScreen paper styles. Net result: on a 360px phone the import dialog is NOT full-screen — it floats as an 85vh card with rounded corners and side margins, defeating the shipped full-screen backbone.
     FIX: Make the paper override responsive: spread BaseDialog's fullscreen behaviour. Either (a) gate the override with useIsMobile() — on mobile drop maxWidth/height/borderRadius and use { height:'100%', borderRadius:0, border:'none' }; or (b) move height:'85vh' into contentSx/an inner Box and let BaseDialog own the paper sx so its full-screen branch survives. Confirm with a 360px render that the dialog goes edge-to-edge.
   - (lines 593-662 (StepIndicator)) Four-step rail (Upload/Map/Assets/Preview) renders all labels inline with 18px circles + connector lines + 0.7rem uppercase mono labels in a single non-wrapping flex row. At 360px the 4 text labels + 3 connectors can exceed the ~296px inner width (px:1.5 inset card), pushing the rail wider than the body and causing horizontal scroll inside the step card.
     FIX: On mobile show numbers only (hide the text labels via display:{xs:'none', sm:'inline-flex'} on the label Typography), keep the connector flex:1 so circles space evenly; or set the rail container to overflowX:'auto' with the current step scrolled into view. Prefer hiding labels under sm.

• src/features/calendar/components/TagFilterDialog.tsx  [dialog, baseDialog=no]
   - (lines 103-115 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) Uses a RAW MUI <Dialog>, not BaseDialog. paperSx has no fullScreen branch and no safe-area insets, so on phones it renders as a centered sm card (calc(100% - 64px) ≈ 296px content with 32px side margins) instead of full-screen. No horizontal overflow, but it ignores the shipped full-screen-on-mobile backbone and wastes vertical space; header/footer don't clear the notch/home-indicator.
     FIX: Convert to BaseDialog (it already maps cleanly: header avatar + title + subtitle, scrollable body, Clear/Cancel/Apply footer via actions + primaryButtonAction). That inherits fullScreen={useIsMobile()} + safe-area padding automatically. If a full rewrite is too large, minimally add fullScreen={useIsMobile()} to the Dialog and merge the fullScreen paper branch ({maxHeight:'100%',borderRadius:0,border:'none'}) into paperSx. The chip grids (flexWrap) already fit 360px.

• src/features/calendar/components/TagFormDialog.tsx  [dialog, baseDialog=no]
   - (lines 393-403 (raw <Dialog maxWidth='sm' fullWidth)) RAW <Dialog>, not BaseDialog — no full-screen on phones, no safe-area insets. Renders as a centered ~296px card. Body is fine width-wise (fields fullWidth, group chips wrap).
     FIX: Add fullScreen={useIsMobile()} and merge the full-screen paper branch into the paperSx (borderRadius:0, border:'none', maxHeight:'100%') + safe-area pt/pb on header/footer; or migrate to BaseDialog. Note the footer (lines 653-731) packs Delete + Cancel + Save (or 'Create another' checkbox + Cancel + Save) in a space-between row — on 360px verify these wrap; set footer flexWrap:'wrap' gap:1 so the delete/secondary control can drop to its own line.

• src/features/calendar/components/TagSelectionDialog.tsx  [dialog, baseDialog=no]
   - (lines 158-170 (raw <Dialog maxWidth='sm' fullWidth, paperSx + maxHeight:'82vh')) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. Content (chips wrap, search field fullWidth, available-list rows are flex space-between) fits 360px without overflow, but it ignores the mobile full-screen backbone.
     FIX: Add fullScreen={useIsMobile()} + merge full-screen paper branch (borderRadius:0/border:none/maxHeight:'100%') and safe-area insets, or convert to BaseDialog. The available-row layout (label chip + meta + actionVerb) uses gap 1.5 with whiteSpace:'nowrap' meta — at 360px a long label + 'N trades' meta + verb could crowd; add minWidth:0 + overflow ellipsis on the label chip.

• src/features/calendar/components/TagSuggestionReviewDialog.tsx  [dialog, baseDialog=no]
   - (lines 158-169 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. Each review row is flex (checkbox + content) with a multiline TextField (fullWidth) — fits 360px. Footer (lines 388-435) is space-between with a left status Typography + Regenerate/Cancel/Save cluster: three buttons + a counter in one row can overflow 360px.
     FIX: Add fullScreen={useIsMobile()} + full-screen paper branch + safe-area, or migrate to BaseDialog (use actions slot for Regenerate). Make the footer wrap: footer sx flexWrap:'wrap', and on mobile hide/relocate the '{n} of {m} selected' status text so the three action buttons fit; or drop the Regenerate label to an icon-only IconButton under sm.

• src/features/calendar/components/CalendarFormDialog.tsx  [dialog, baseDialog=no]
   - (lines 282-292 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. This is a long, scrollable form (Identity/Capital/Assets/Targets/Risk). On a phone it renders as a centered ~296px card constrained to maxHeight:'70vh', wasting screen and creating a small scroll viewport instead of the full-screen treatment the backbone provides. Internal grids are already responsive (Capital/Targets/Risk all use xs:'1fr' or gridTemplateColumns:{xs:'1fr', sm:...}).
     FIX: Add fullScreen={useIsMobile()} to the Dialog, merge the full-screen paper branch into paperSx (borderRadius:0, border:'none', maxHeight:'100%'), drop the body maxHeight:'70vh' when full-screen (let the body flex), and add safe-area pt/pb to header/footer; or migrate to BaseDialog with contentSx for the scroll body. Grids need no change.

• src/features/calendar/components/ImageZoomDialog.tsx  [dialog, baseDialog=no]
   - (lines 191-213 (raw <Dialog maxWidth='md' fullWidth, PaperProps.sx.maxWidth:'80%')) RAW <Dialog> lightbox with PaperProps maxWidth:'80%'. On a 360px phone the image viewer is capped to ~288px wide and centered with side gutters — a zoom/lightbox should be full-bleed on mobile. The 80vh image container + absolute nav arrows at left:16/right:16 then sit inside a narrow card. Wheel-zoom + mouse-drag pan are desktop-only interactions with no touch pinch handling. Net: cramped, letterboxed image on phones.
     FIX: Make it full-screen on mobile: add fullScreen={useIsMobile()} and on mobile set PaperProps.sx maxWidth:'100%', width:'100%', height:'100%', m:0, borderRadius:0, background transparent/solid as today. Keep the 80vh container only on desktop (height:{xs:'100%', sm:'80vh'}). Consider touch-action and pinch-zoom for phones, but at minimum remove the 80% cap so the image uses the full viewport width.

• src/features/calendar/components/dialogs/CopyTradeDialog.tsx  [dialog, baseDialog=no]
   - (lines 141-149 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. Content is simple and responsive (destination rows are FormControlLabel flex with flex:1 minWidth:0 label, status icon trailing; scroll list maxHeight:320). Fits 360px width-wise but ignores the mobile full-screen backbone.
     FIX: Add fullScreen={useIsMobile()} + merge full-screen paper branch into paperSx + safe-area insets, or convert to BaseDialog (header/body/footer map directly; Cancel/Copy -> footer). No inner layout changes needed.

• src/features/calendar/components/dialogs/CalendarLinkDialog.tsx  [dialog, baseDialog=no]
   - (lines 107-119 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. Body (explainer strip, Select target, linked-calendar row with Unlink button) is responsive and fits 360px. The 'Currently linked' row (lines 226-270) is a space-between flex with calendar name (ellipsis) + Unlink button (flexShrink:0) — fits.
     FIX: Add fullScreen={useIsMobile()} + full-screen paper branch + safe-area, or migrate to BaseDialog. No inner width fixes required.

• src/features/calendar/components/dialogs/DuplicateCalendarDialog.tsx  [dialog, baseDialog=no]
   - (lines 70-82 (raw <Dialog maxWidth='sm' fullWidth slotProps.paper=paperSx)) RAW <Dialog>, not BaseDialog — no full-screen/safe-area on phones. Body is two full-width choice buttons (Settings only / Settings & trades) — fully responsive, fits 360px.
     FIX: Add fullScreen={useIsMobile()} + full-screen paper branch + safe-area insets, or convert to BaseDialog. No inner layout changes needed.

• src/features/performance/components/PerformanceHeader.tsx  [component, baseDialog=na]
   - (lines 70-83 (h1 title Typography)) Title font is a fixed fontSize: '1.85rem' (~29.6px) with letterSpacing -0.025em and no responsive step. Long period+calendar titles (e.g. 'September 2026') plus the controls row competing for width can crowd the 328px content area; the fixed large type does not scale down on phones.
     FIX: Make the size responsive: fontSize: { xs: '1.4rem', sm: '1.85rem' }. Title already has minWidth:0 on its wrapper (line 68) so it can shrink; this just prevents the oversized type on narrow screens.
   - (lines 57-67 + 97-107 (outer flex row + controls cluster)) Outer Box is flexWrap:'wrap' so the controls drop below the title on narrow widths — good. But the controls cluster (PeriodPicker + PerfPill, lines 97-107) is also flexWrap:'wrap' with no full-width treatment; on 360px the 5-option PerfPill (Month/Quarter/YTD/Year/All) plus the PeriodPicker (minWidth 96 + two 28px arrows ≈ 152px) can exceed 328px and wrap awkwardly mid-cluster, and the pill itself is inline-flex fit-content (no horizontal scroll), so the 5 segments may overflow the row.
     FIX: On mobile give the controls row full width and let PerfPill stretch: wrap controls Box with width:{xs:'100%',sm:'auto'} and justifyContent:{xs:'space-between',sm:'flex-end'}, and pass fullWidth to PerfPill below sm (e.g. fullWidth={useIsMobile()}). Alternatively render PerfPill segments in a horizontally-scrollable container below sm. Verify the 5 segments fit at 360px after the change.

• src/features/performance/components/charts/DailySummaryTable.tsx  [component, baseDialog=na]
   - (lines 118-271 (Table) + 272-290 (TablePagination)) A 4-column MUI Table (Date / Trades / Session / P/L). Date cell renders 'dd/MM/yyyy' and Session can be a word; at 360px four columns plus padding will exceed the ~328px content area. The TableContainer has overflow:'auto' (line 121) so it scrolls rather than breaking the page — acceptable for no-page-overflow — but a horizontally-scrolling table is a poor mobile UX. Worse, TablePagination (rowsPerPageOptions + 'Rows per page' label + range + nav buttons, lines 272-290) is notoriously wide and can itself overflow 328px and force horizontal scroll on the whole card.
     FIX: Two parts. (1) Table: the TableContainer overflow:'auto' already prevents page overflow; optionally below sm render as stacked rows/cards (date as header, Trades/Session/P&L as label:value rows) via useIsMobile() for a real mobile layout. (2) TablePagination: on xs hide the rows-per-page selector to reclaim width — add sx '.MuiTablePagination-selectLabel, .MuiTablePagination-input, .MuiTablePagination-select': { display: { xs: 'none', sm: 'flex' } } (or pass rowsPerPageOptions={[]} on mobile), and add '.MuiTablePagination-toolbar': { pl: { xs: 1 }, flexWrap: 'wrap' } so it never overflows 328px.

• src/features/performance/components/charts/TagPerformanceAnalysis.tsx  [component, baseDialog=na]
   - (lines 117-177 (headerRight: two filter buttons) inside CardShell head right-slot) Header right-slot packs two outlined buttons ('Select Primary Tags' / 'Select Secondary Tags', each ~150-170px). They are in a flexWrap:'wrap' Box (good) but combined with the card title 'Tag Performance Analysis' in the header band, on 360px this is very cramped and the buttons wrap to two stacked rows competing with the title. Needs verification that CardShell head stacks title-above-controls on mobile.
     FIX: Below sm, move headerRight out of the head right-slot into the card body as a full-width row, OR set the buttons to fullWidth/condensed labels ('Primary'/'Secondary') on xs via useIsMobile(). Ensure the controls Box is width:{xs:'100%'} and the buttons flex:1 so two buttons share the 328px row evenly.
   - (lines 244-394 (ResponsiveContainer BarChart) + margin left:20 right:30) Chart width is fluid so no page overflow. But X-axis dataKey='tag' tick fontSize 12 with multiple selected tags will collide/overlap on a ~328px-wide chart (no angle, no interval). Legend (line 318) is default recharts — usually fits. The chart margin {left:20,right:30} eats ~50px of an already narrow width.
     FIX: On mobile reduce chart margins (left:0,right:8), and angle the X-axis ticks (angle={-35} textAnchor='end' height={50}) or set interval={0} with smaller fontSize so tag labels remain legible at 360px. No page overflow risk, but readability fix.

• src/features/performance/components/charts/TagDayOfWeekAnalysis.tsx  [component, baseDialog=na]
   - (lines 158-240 (headerRight: FOUR controls)) Header right-slot has FOUR buttons — 'Win Rate', 'P&L', 'Select Primary Tags', 'Select Secondary Tags' — in a flexWrap Box. On 360px this wraps into 2-3 rows and badly crowds the card title 'Tag Performance by Day of Week' in the header band. This is the worst header-control density in the pillar for mobile.
     FIX: Below sm, relocate these controls from the CardShell head right-slot into a dedicated full-width body toolbar: a metric toggle pair (Win Rate/P&L) as a segmented control row + the two tag-filter buttons as a flex row with flex:1 each. Use useIsMobile() to switch head.right to undefined and render the toolbar at the top of the body. Shorten labels to 'Primary'/'Secondary' on xs.
   - (lines 338-393 (ResponsiveContainer BarChart) margin left:20 right:30) Fluid width (no overflow) but X-axis 'day' labels (Mon..Fri) are few so they fit; Y-axis tickFormatter shows currency on P&L mode. Margins left:20/right:30 waste ~50px on a narrow chart. Legend is default.
     FIX: Reduce margins on mobile (left:0,right:8). Day labels are short so no rotation needed. Minor.

• src/components/common/RichTextEditor/components/EditorToolbar.tsx  [component, baseDialog=na]
   - (renderToolbarContent() lines 610-882 — `<Toolbar variant="dense" sx={{ flexWrap: 'nowrap', ... }}>`) The sticky/floating toolbar lays out ~8 button groups (Bold/Italic/Underline = 3×36px, Heading 36+arrow, 2 list buttons 72px, Trade 36, Callout 36+arrow, Color 36, Link/Image/Clear 108px) plus 6 vertical dividers in a single `flexWrap:'nowrap'` row with `p:1`. That is well over 500px of intrinsic width and WILL overflow horizontally at 360px. This is the exact Draft.js-toolbar-must-wrap-or-scroll case. The sticky variant container (line 932) has no overflow handling, so the whole editor column blows past the viewport.
     FIX: Two-part: (1) On the sticky variant wrapper (lines 932-947) add `overflowX:'auto'`, `scrollbarWidth:'none'`, `'&::-webkit-scrollbar':{display:'none'}` so the row scrolls horizontally on phones instead of forcing page width. (2) Better UX: on the Toolbar sx set `flexWrap: { xs: 'wrap', sm: 'nowrap' }` via useIsMobile() so on phones the buttons wrap to a second line. Keep button tap targets at the existing 36px (note: 36px < the 44px tap-target floor — bump `minWidth`/`height` to 40-44 on xs). The floating variant should be force-disabled on mobile (selection toolbars don't work with touch selection) — gate `variant==='floating'` rendering behind `!useIsMobile()` and rely on the sticky toolbar.
   - (ToggleButton/IconButton sizing throughout (e.g. lines 632-633 `minWidth:'36px', height:'36px'`, 688-691, 757-759, 815-818)) Toolbar tap targets are 36px square — below the 44px minimum touch target. Hard to hit reliably on a phone.
     FIX: Make height/minWidth responsive: `minWidth:{xs:44,sm:36}, height:{xs:44,sm:36}` (or 40 minimum). Apply to the shared button sizing in the ToggleButtonGroup sx blocks and the standalone IconButtons.
   - (renderColorMenu() line 268 `width: 180` and heading/callout Menus) Color menu Paper is fixed width:180 — fine at 360px (fits within 328). Not an overflow risk; flagged only to confirm it was checked.
     FIX: No change needed (180 < 328). Leave as-is.

• src/features/notes/components/sidePanel/NotesContent.tsx  [component, baseDialog=na]
   - (lines 294-364 — Search and Filter Row (`display:'flex', gap:1, alignItems:'center'`, no flexWrap)) This row packs a flex:1 search TextField + a creator Select with `minWidth:140` + a 2-button ToggleButtonGroup (~80px) into a non-wrapping row. This renders inside NotesDrawer which is `width:{xs:'100%'}` (full 360px). Fixed content = ~140 (select) + ~80 (toggles) + gaps + adornment ≈ 250px, leaving ~70px for search — the search field collapses to near-unusable and the row can overflow because the Select has a hard minWidth.
     FIX: Stack on phones: `flexDirection:{xs:'column', sm:'row'}` and `alignItems:{xs:'stretch', sm:'center'}` on the row Box (line 294). Give the search its own full-width line; put the creator Select + view-mode toggles on a second row with `display:'flex', gap:1, justifyContent:'space-between'`. Drop the Select `minWidth:140` to `minWidth:{xs:0, sm:140}` and let it flex. Alternatively wrap with `flexWrap:'wrap'` as a cheaper fix.
   - (line 326 `<FormControl size="small" sx={{ minWidth: 140 }}>` (creator filter)) Hard 140px minWidth on the creator Select contributes to the row-overflow above.
     FIX: Change to `minWidth:{xs:0, sm:140}` and let it participate in the wrapped/stacked layout.

• src/components/common/RichTextEditor/components/ImageUploadDialog.tsx  [dialog, baseDialog=no]
   - (lines 294-307 — raw `<Dialog maxWidth="md" fullWidth {...dialogProps}>` (no fullScreen prop at all)) This is a RAW <Dialog> that NEVER goes full-screen. With maxWidth='md' fullWidth it relies on default dialog margins; on a 360px phone the Paper is ~360 minus default 32px dialog margin = ~328 usable, which mostly fits, BUT the dialogProps/paperSx from useDialogTokens may set a fixed width/margin not tuned for 360. It does not get BaseDialog's mobile full-screen treatment, so on small phones it shows a cramped windowed dialog instead of full-screen.
     FIX: Add `fullScreen={useIsMobile()}` to the Dialog and, when full-screen, zero the Paper margin + add safe-area insets. Best: migrate to BaseDialog (it auto-full-screens on phones). Verify paperSx from useDialogTokens doesn't pin a width > 328 on xs.
   - (lines 241-292 renderTabs() — `display:'inline-flex'` pill row Upload/URL/Unsplash, each px:1.5) Three pills in a non-wrapping inline-flex. Roughly 3×~70px + gaps ≈ 220px — fits 328, so OK. Flagged to confirm checked.
     FIX: No change required; fits at 360px.
   - (lines 606-663 footer (`...footerSx, justifyContent:'space-between'`) — caption text + Cancel/Insert buttons) Footer puts a left help caption (e.g. 'Direct URL — make sure hotlinking is allowed') and right-side Cancel + 'Insert image' button in a space-between row. On 360px the long caption + two buttons can collide / overflow since neither side wraps.
     FIX: On xs stack the footer: `flexDirection:{xs:'column', sm:'row'}, alignItems:{xs:'stretch', sm:'center'}, gap:1`, and make the help caption `display:{xs:'none', sm:'inline-flex'}` (it's non-essential chrome) so the action buttons get the full width on phones.
   - (line 576-583 Unsplash tab — UnsplashImagePicker with `resultsHeight={340}, tileHeight={110}`) Fixed tile/results heights are fine vertically; the picker's internal grid (not in this file) needs to be column-responsive but that's outside this surface. The 340px results panel is acceptable for a full-screen mobile dialog.
     FIX: No change here; ensure UnsplashImagePicker grid is responsive separately (out of scope for this file).

• src/pages/EconomicEventsPage.tsx  [page, baseDialog=na]
   - (lines 81-85, 519-528) APP_HEADER_HEIGHT is hardcoded to 64 and used as APP_VIEWPORT_OFFSET to compute height: calc(100vh - 64px) on md+. On phones the AppHeader is 56px (HEADER_HEIGHT xs), so the constant is wrong for mobile — but more importantly the height calc is md-only here so the page height is 'auto' on xs, which is correct. The hardcoded 64 should instead import HEADER_HEIGHT/BELOW_HEADER_HEIGHT from styles/layout.ts so it tracks the 56/64 token split rather than duplicating a magic number.
     FIX: Replace `const APP_HEADER_HEIGHT = 64` with the BELOW_HEADER_HEIGHT token import from 'styles/layout' and use the responsive value, e.g. height: { xs: 'auto', md: `calc(100vh - ${BELOW_HEADER_HEIGHT.sm}px)` }.
   - (lines 547-631 (page head) + 592-630 (right action Stack)) Page-head is a flex row with `justifyContent: space-between` and `flexWrap: wrap`. The right cluster is a non-wrapping `Stack direction='row'` holding the FilterPill (High/Med/Low segmented control ~140px) plus the notifications IconButton. The FilterPill itself does not wrap. On a 328px content area the title block + this row mostly fit because of flexWrap on the parent, but the FilterPill segmented control is borderline; combined with the h1 at 1.5rem it is tight but does not overflow. Acceptable, but the FilterPill row should be allowed to drop full-width.
     FIX: Add `flexWrap: 'wrap'` + `rowGap: 1` to the right Stack (line 592) or set the parent head to `flexDirection: { xs: 'column', sm: 'row' }, alignItems: { xs: 'stretch', sm: 'flex-end' }` so on xs the title and the filter/bell row stack instead of competing for one line.
   - (lines 704-778 (body grid) + 742-776 (sidebar Stack)) Body grid is `gridTemplateColumns: { xs: '1fr', md: '200px 1fr', lg: '200px 1fr 320px' }` — single column on xs, which is correct. But the EventList card (line 1307-1309) sets `minHeight: { xs: 360 }` and the page height is 'auto' on xs, so the day-rail (horizontal scroll row), event list (360px min), impact-distribution card, and pinned card all stack vertically — that is a very long scroll but no horizontal overflow. OK structurally; flagged only because the day-rail forces a 152px-min horizontal scroll strip (see DayRail issue) which is the one place a phone user must scroll sideways inside the page.
     FIX: No overflow fix needed for the grid itself. Address the DayRail horizontal-scroll item separately (below). Confirm the single-column stack is intended on phones.
   - (lines 1149-1161 (DayRail ButtonBase)) The page-local DayRail (xs flex-direction:'row' with overflowX:'auto') renders each day chip at `minWidth: { xs: 152 }`. Seven 152px chips = ~1064px of horizontal scroll content inside a 328px viewport. It is contained by `overflowX: 'auto'` (line 1127) so it does NOT break the page, but it forces a sideways scroll strip and only ~2 days are visible at once. EconomicEventsView.tsx already has a better mobile DayRail (`layout='row'` with 7 equal flex:1 cards that compress to fit, lines 387-553) — this page should reuse that compressing row variant instead of the 152px-min scroller.
     FIX: On xs, render the DayRail as the compressing 7-up row used in EconomicEventsView (flex:1 per day, minWidth:0, no per-item fixed minWidth, no overflowX scroll). Gate via useIsMobile() and drop the `minWidth: { xs: 152 }` so all 7 days fit the 328px width.
   - (lines 633-647 (CurrencyChips Stack) + 937-991 (CurrencyChips)) Currency chips wrap (flexWrap:'wrap', rowGap) — 9 currency pills + Clear at ~44px each wrap to 3-4 rows on 328px. No overflow. Tap targets: each chip is py:0.5 (~28px tall) — under the 44px minimum touch target.
     FIX: Bump chip vertical hit area on mobile: py: { xs: 0.875, sm: 0.5 } or add minHeight: { xs: 36 } to the ButtonBase so chips are easier to tap. Functional, low severity.
   - (lines 649-700 (hub tabs + date controls Stack)) Row holds HubTabs (3-segment control), a flex spacer, prev/Today/next nav, and the DatePicker trigger (week-range label like 'May 5 – May 11, 2026'). flexWrap:'wrap' is set so it wraps, but the DatePickerTrigger label is long and on xs the segmented HubTabs (~190px) + nav cluster + date pill compete. With wrap it reflows to 2-3 rows — no overflow but visually busy.
     FIX: Acceptable with the existing flexWrap. Optionally shorten the date label on xs (start–end without year, already done in EconomicEventsView's weekRangeLabel) and let the spacer `Box sx={{ flex: 1 }}` collapse on xs so groups pack left.

• src/components/notifications/ClearAllConfirmDialog.tsx  [dialog, baseDialog=no]
   - (whole file — delegates to ConfirmationDialog (components/common/ConfirmationDialog.tsx)) ClearAllConfirmDialog renders ConfirmationDialog, which uses a RAW <Dialog maxWidth='xs' fullWidth> (ConfirmationDialog.tsx lines 78-86) — it does NOT use BaseDialog and does NOT set fullScreen on mobile. So unlike BaseDialog-based dialogs, this confirm dialog renders as a centered xs card on phones. An xs dialog (~360-444px target, fullWidth) on a 360px viewport is bounded by MUI's default 32px (16px each side) margin, so it lands at ~328px and does not overflow horizontally — but it is the lone confirm dialog NOT following the new full-screen-on-phone pattern, creating an inconsistent UX (every other dialog goes full-screen, this one floats).
     FIX: Migrate ConfirmationDialog to BaseDialog (pass title, message as children, and wire confirm/cancel to primaryButtonAction/cancelButtonAction with confirmColor → primary button accent) so it inherits useIsMobile() full-screen + safe-area insets. If a full migration is out of scope, add `fullScreen={useIsMobile()}` plus the same `maxHeight:'100%', borderRadius:0` paper overrides to the raw Dialog in ConfirmationDialog.tsx. This is shared infra so the fix benefits every ConfirmationDialog caller.

• src/features/events/components/charts/EconomicEventCorrelationAnalysis.tsx  [component, baseDialog=na]
   - (lines 228-237 (Currency FormControl minWidth:140) inside header controls (lines 220-239)) CardShell head `right` renders RoundedTabs (High/Medium) + a `FormControl size='small' sx={{ minWidth: 140 }}` Select side-by-side. On a 328px CardShell header that also holds the title/eyebrow, the 140px-min Select plus the tabs overflow the header band on phones. CardShell header is typically a flex row; without a wrap/stack override this pushes content past 328px.
     FIX: Make header controls stack on xs: wrap the RoundedTabs + Select in a Box with `flexDirection: { xs: 'column', sm: 'row' }, alignItems:'stretch', gap:1` and set the FormControl to `minWidth: { xs: 0, sm: 140 }, width: { xs: '100%', sm: 'auto' }`. Confirm CardShell's head band allows wrapping (flexWrap) on xs.
   - (lines 510-575 (most-common-events sub-card header) + 558-573 (search TextField width:240)) Sub-card header is a flex row (flexWrap:'wrap' is set, line 520) holding the title block + a ToggleButtonGroup (Individual/Grouped) + a `TextField sx={{ width: 240 }}`. The fixed width:240 search field plus the toggle group exceed 328px; flexWrap lets them drop to a new line but the 240px field still forces that row to be 240px wide minimum — fits within 328 but leaves almost no margin and the toggle group + field won't sit together.
     FIX: Set the search field to `width: { xs: '100%', sm: 240 }` and let the controls Box be `flexDirection: { xs: 'column', sm: 'row' }, alignItems:'stretch'`. Make the ToggleButtonGroup full-width on xs too.
   - (lines 583-588 (event tiles grid)) Event tiles grid is `gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))'`. minmax(280px,...) means each tile demands at least 280px. On a 328px content area that yields exactly one column (OK, no overflow) BUT if this card is nested inside another padded container the available width can drop below 280, forcing the tile to 280px and overflowing the parent.
     FIX: Lower the floor on phones: gridTemplateColumns: { xs: '1fr', sm: 'repeat(auto-fill, minmax(280px, 1fr))' } so xs is an explicit single full-width column and never demands 280px.
   - (lines 458-499 (summary + compare grids)) Summary and compare grids are `{ xs: '1fr', md: '1fr 1fr' }` — single column on xs, good. Inner StatTile pair grids are fixed '1fr 1fr' (lines 350, 720) which is 2-up at any width; at 328px each cell is ~150px which holds a count + percent fine. No overflow.
     FIX: No change — these are already responsive.

• src/components/common/ConfirmationDialog.tsx  [dialog, baseDialog=no]
   - (lines 78-86 (raw Dialog, no fullScreen)) Shared confirm dialog uses a RAW <Dialog maxWidth='xs' fullWidth> with no `fullScreen={useIsMobile()}` and no full-screen paper overrides. It is the backing component for ClearAllConfirmDialog and likely many other confirm flows. Every BaseDialog-based dialog now goes full-screen on phones; this one floats as a centered xs card. At 360px the xs+fullWidth card resolves to ~328px (within MUI's 16px margins) so it does NOT overflow, but it is the inconsistency / pattern-break the audit should fix at the source.
     FIX: Either (a) refactor ConfirmationDialog to compose BaseDialog (title + children body + primary/cancel buttons via primaryButtonAction/cancelButtonAction, mapping confirmColor to the primary accent), inheriting full-screen + safe-area insets; or (b) add `fullScreen={useIsMobile()}` and, when full-screen, the `{ maxHeight:'100%', borderRadius:0, border:'none' }` paper overrides plus safe-area top/bottom padding on header/footer — mirroring BaseDialog lines 84-97 / 157. Fixing here propagates to ClearAllConfirmDialog and all other callers.

• src/features/orion/components/aiChat/ChatMessage.tsx  [component, baseDialog=na]
   - (User-message edit action (lines 500-529): position:'absolute', left:-36) The hover Edit icon is absolutely positioned at left:-36 — 36px OUTSIDE the message bubble's left edge. The user bubble is right-aligned with maxWidth:'78%', so at 360px the bubble starts around x=70-80px and the edit button at left:-36 lands near the viewport's left gutter but on a very full bubble it can push to negative x and clip / cause horizontal overflow. More importantly it's hover-only (opacity:0, reveal on '.msg-actions' hover) which never triggers on touch — the edit affordance is unreachable on phone.
     FIX: On useIsMobile() move the edit action inline below/inside the bubble (e.g. left:'auto', right:8, bottom:-28, or render a small always-visible edit IconButton in the metadata row at line 533). Drop the hover-gating on touch (replace opacity:0 with a tap target). This also removes the negative-offset overflow risk.
   - (User bubble maxWidth:'78%' (line 443) + assistant gap:2 with 26px avatar (lines 276-294)) 78% of 328px ≈ 256px for the user bubble — fine. Assistant row uses gap:2 (16px) + 26px avatar, leaving ~286px for content (flex:1, minWidth:0 — good). Tables/code inside are handled by child renderers. No overflow from this file itself.
     FIX: No change; the embedded table/wide-content overflow risk lives in MarkdownRenderer / HtmlMessageRenderer (flagged separately).

• src/features/orion/components/aiChat/MarkdownRenderer.tsx  [component, baseDialog=na]
   - (Table renderer (lines 40-96): TableContainer overflow:'auto' but inner Table sx minWidth:300; th/td px:2 py:1.5) Table has minWidth:300 inside an overflow:'auto' container — so the container CAN scroll, but the container itself is the chat content column (~286px usable on a 360px phone). minWidth:300 > 286 means a 1-column-of-table horizontal scrollbar appears INSIDE the message, which is acceptable (contained, not page overflow) — BUT a wide multi-column briefing table at px:2 (16px) per-cell padding blows the intrinsic width far past 300, and while the TableContainer scrolls, the generous py:1.5/px:2 cell padding wastes the tiny width. The page won't overflow (container clips), so this is a usability rather than overflow defect.
     FIX: On useIsMobile() reduce th/td padding to px:1 py:0.75 and fontSize to 0.78rem so more columns fit before the inner scroll kicks in. Confirm the TableContainer has maxWidth:'100%' (it does) so the horizontal scroll stays contained to the bubble. Acceptable as-is for the no-page-overflow rule; tighten padding for readability.

• src/features/orion/components/aiChat/AIChatMentionInput.tsx  [component, baseDialog=na]
   - (Mention/slash Popper Paper (lines 696-705): minWidth:280, maxWidth:420) The slash/@-mention popup Paper has a hard minWidth:280. At a 360px viewport with the input sitting inside the chat footer (which has p:2 = 16px gutters), the available width is ~328px, so 280 fits — but the Popper is anchored top-start to the editor container which itself can be inset further (image button + send button share the row). If the editor anchor is narrow, the 280px popup can extend past the right viewport edge since Popper has no boundary/flip modifier set here, causing horizontal overflow.
     FIX: On useIsMobile() set minWidth to 'min(280px, calc(100vw - 24px))' and add a popper modifier (preventOverflow boundary:'viewport' / flip) so the menu stays within the screen. Also cap maxWidth at 'calc(100vw - 24px)'.
   - (Editor content maxHeight maxRows*1.6em (line 671) and chip rows) Editor wraps and scrolls vertically (overflowY:auto, wordBreak:break-word) — good, no horizontal overflow. Tag/note chips are inline-flex and wrap (flexWrap:'wrap' on container line 663).
     FIX: No change to the editor body; only the Popper width/flip fix above is required.

• src/features/orion/components/settings/CustomToolsSection.tsx  [dialog, baseDialog=no]
   - (Delete-confirm Dialog (lines 321-352): raw <Dialog maxWidth='xs' fullWidth>) The delete-confirmation uses a RAW MUI <Dialog> (not BaseDialog), so it does NOT inherit the full-screen-on-phone + safe-area behavior. maxWidth='xs' renders a centered modal; on a 360px phone xs (~444px) clamps to width-32px so it fits horizontally, but it floats mid-screen rather than the app's standard mobile dialog treatment, and its DialogActions/title get none of the safe-area inset handling.
     FIX: Replace the raw <Dialog> with the project's BaseDialog (or ConfirmationDialog, which OrionTasksContent already uses for the same pattern) so it goes full-screen with safe-area insets on phone and stays visually consistent. Reuse ConfirmationDialog — it's a drop-in for this exact title+message+confirm/cancel shape.
   - (Header row (lines 197-247): title block + 'Add tool' Button, justify space-between) Title 'Custom tools' + caption '{n} of 5 used' on the left and a contained 'Add tool' button on the right. Fits 328px; no overflow.
     FIX: No change needed.

• src/features/orion/components/settings/StepRail.tsx  [component, baseDialog=na]
   - (Step row (lines 28-74): non-wrapping flex of [circle + mono UPPERCASE label] x4 with connector lines) CREATE mode renders 4 steps — 'DESCRIBE', 'REVIEW', 'SECRET', 'VERIFY' — each as a circle (18px) + mono uppercase label (letterSpacing 0.14em, fontSize 0.7rem) joined by connector lines (flex:1 maxWidth:56). With 4 labels + 3 connectors + gaps (1.25) the intrinsic width far exceeds 328px and there is NO flexWrap, so the rail overflows horizontally inside the settings dialog on a 360px phone. The connectors have flex:1 so they can shrink, but the 4 fixed-width uppercase labels cannot, forcing horizontal scroll/overflow.
     FIX: On useIsMobile() collapse the rail to a compact form: either (a) render only the active step's label + 'Step X of N' text and hide the inactive labels (keep all circles), or (b) hide labels entirely on xs and show just numbered circles + connectors, or (c) set the outer Box to overflowX:'hidden' and render labels below circles in a wrapped column. Simplest robust fix: on xs, drop the per-step Typography labels (show circles only) and add a single 'Step {current+1} of {steps.length}: {steps[current]}' line above the rail.

• src/components/heroImage/ImagePickerDialog.tsx  [dialog, baseDialog=no]
   - (lines 66-78 (<Dialog ... maxWidth='md' fullWidth>)) Uses a RAW MUI <Dialog>, NOT BaseDialog, so it does NOT inherit the new automatic full-screen-on-phone behavior. MuiDialog theme override (theme.ts 295-302) only sets radius/shadow — no responsive fullScreen. Default MUI paper has margin:32 + maxWidth:calc(100%-64px), so at 360px the dialog is only ~296px wide with 32px dead margins all around, wasting scarce phone width for an image grid. The body maxHeight:'75vh' + footer space-between ('Photos by Unsplash' link + Cancel) get cramped.
     FIX: Add fullScreen on phones. Either (a) migrate to BaseDialog (preferred — inherits full-screen + safe-area insets), or (b) keep the raw Dialog and add fullScreen={useIsMobile()} from hooks/useResponsive, plus slotProps.paper.sx margin:0 / maxWidth:'100%' / height:'100%' on mobile and let the body flex to fill. Reduce header/footer px to 2 on xs.
   - (lines 104-118 (body) referencing UnsplashImagePicker resultsHeight default 420) Inner picker renders with default resultsHeight=420 and tileHeight=130; at ~296px (or even full-screen 360) the default columns {xs:2} give ~140px tiles which is fine, but the fixed 420px results panel inside a 75vh-capped body on a short phone leaves little room for the search row + chips.
     FIX: When full-screen on mobile, pass resultsHeight as a larger flex value or let the picker fill: e.g. pass columns={{xs:2}} (already default) and consider resultsHeight tied to viewport. Minimum: ensure the dialog goes full-screen (above fix) so the 420px grid + search + footer all fit without inner clipping.

• src/components/common/ConfirmationDialog.tsx  [dialog, baseDialog=no]
   - (lines 78-86: raw <Dialog maxWidth='xs' fullWidth> with slotProps paper: paperSx (from useDialogTokens, fixed borderRadius:2, NO full-screen)) Uses a RAW MUI <Dialog>, not BaseDialog, so it does NOT inherit the new phone full-screen behavior. paperSx (dialogTokens.ts lines 73-79) is a fixed rounded card with no xs override. On a 360px phone this renders as a small floating card (fine width-wise via xs+fullWidth ≈ 296px) but breaks the app-wide convention that dialogs go full-screen on phones, and gives no safe-area insets. This is the most-reused confirm dialog in the app, so the inconsistency is high-visibility.
     FIX: Migrate to BaseDialog (preferred) so it inherits full-screen + safe-area on phones, OR add full-screen manually: const isMobile = useIsMobile(); on <Dialog> add fullScreen={isMobile}; and merge into paperSx an xs override { borderRadius: isMobile ? 0 : 2, m: isMobile ? 0 : undefined, maxHeight: isMobile ? '100vh' : undefined }.
   - (lines 124-142: footer Box (footerSx) justify flex-end with two side-by-side buttons) footerSx (dialogTokens.ts lines 103-112) lays cancel+confirm in a row justify flex-end. With long custom confirmText/cancelText (callers pass arbitrary strings) two buttons + gap can crowd a ~296px footer; primaryButtonSx px:1.75 keeps tap targets but text may truncate/overlap.
     FIX: Acceptable for short labels; if migrating to full-screen, let the footer wrap: add flexWrap:'wrap' (or on xs stack the buttons with a fullWidth primary). Pair with the full-screen fix above.

• src/components/heroImage/ImagePickerDialog.tsx  [dialog, baseDialog=no]
   - (lines 66-78: raw <Dialog maxWidth='md' fullWidth> with paperSx (no full-screen)) RAW MUI <Dialog>, not BaseDialog — no phone full-screen. maxWidth='md' + fullWidth renders at calc(100% - 64px) ≈ 296px on a 360px phone (32px margins each side) which fits, but a 'md' image-picker squeezed to 296px with 32px wasted margins is a poor cover-image-selection UX on phones, and the inner UnsplashImagePicker grid is the real overflow risk.
     FIX: Migrate to BaseDialog for inherited full-screen, OR add const isMobile = useIsMobile(); fullScreen={isMobile} and an xs paperSx override (borderRadius:0, m:0, maxHeight:'100vh'). On phones a full-screen picker reclaims the wasted side margins for the image grid.
   - (lines 104-118: body Box px:2.5 py:2 maxHeight:'75vh' wrapping <UnsplashImagePicker showCacheStats showClearCache>) The UnsplashImagePicker child (out of file scope) likely renders a multi-column thumbnail grid plus a 'cache stats' row + 'clear cache' control. A fixed multi-col grid or a stats row laid out row-wise is the concrete 360px overflow vector here. maxHeight:'75vh' is fine.
     FIX: Audit components/heroImage/UnsplashImagePicker.tsx: ensure its image grid is gridTemplateColumns:{xs:'repeat(2,1fr)',sm:'repeat(3,1fr)'} (or auto-fill minmax) and the cache-stats/clear-cache controls wrap (flexWrap:'wrap'). Flag that file for follow-up.

• src/components/sidePanel/SidePanel.tsx  [component, baseDialog=na]
   - (lines 19, 36-58 (PANEL_WIDTH const, outer Box width, inner Box minWidth)) Line 19 + 55: PANEL_WIDTH = clamp(340px, 28vw, 450px) and the inner content Box sets minWidth: PANEL_WIDTH. SidePanel is meant for lg+ desktop, BUT AppLayout.tsx line 364 renders <SidePanel> INSIDE a UnifiedDrawer on mobile (!isLgUp). On a 360px phone the drawer is 100% width (~360px), 28vw resolves to ~100px so clamp pins to the 340px floor. minWidth:340 inside a ~360px drawer (minus 1px drawer borderLeft + paper border) is right at the edge and OVERFLOWS at the 320px floor (340 > 320). This is a hard horizontal-overflow violation.
     FIX: Make the width responsive to the host. Change PANEL_WIDTH usage so on phones it is 100%: set width and minWidth to { xs: '100%', lg: 'clamp(340px, 28vw, 450px)' } and remove the unconditional minWidth:340. Simplest: import useIsCompact/useIsDesktop from hooks/useResponsive and when not desktop use width:'100%', minWidth:0. Drop the inner Box minWidth entirely (it only exists to prevent squish during the desktop width transition; gate it behind isDesktop).
   - (lines 60-64 (SidePanelHeader render) cross-referenced with AppLayout.tsx 352-365) Lines 60-64: When SidePanel is hosted inside UnifiedDrawer on mobile (AppLayout 352-365), SidePanelHeader renders a SECOND header (title + close) directly below UnifiedDrawer's own header (which already shows the same title + close icon). Two stacked headers with two close buttons on a 360px drawer wastes vertical space and confuses the tap targets.
     FIX: Add an optional `hideHeader` prop to SidePanel (and pass it from AppLayout's drawer host at line 364) so the inner SidePanelHeader is suppressed when the UnifiedDrawer already supplies chrome. Alternatively have the drawer host render SidePanel content without the wrapper header.

• src/features/calendar/components/sidePanel/SearchContent.tsx  [component, baseDialog=no]
   - (lines 668-681 (raw Dialog), 944-980 (DatePickers with desktopPaper)) FilterDialog (lines 668-1012) is a RAW <Dialog maxWidth="sm" fullWidth> — NOT BaseDialog. It does not go full-screen on phones, so it renders as a centered ~296px card. Inside it: a 3-segment date control (line 897-942, three buttons flex:1) and two MUI X DatePickers (lines 944-980). DatePicker defaults to desktopPaper/popper here (desktopPaper sx is set, lines 952/977) which forces the desktop calendar popper — on a 360px phone the desktop date popper can overflow the viewport horizontally.
     FIX: Convert FilterDialog to BaseDialog (inherits full-screen + safe-area on phones). For the DatePickers, let MUI pick the mobile variant on phones (remove the forced desktopPaper slot or wrap so the responsive DatePicker renders MobileDatePicker below sm), or explicitly use MobileDatePicker via useIsMobile so the calendar opens as a full-screen modal instead of an overflowing popper.
   - (lines 187-240 (Filters button), 470-540 (pagination IconButtons)) Search + Filters row (lines 156-241): the Filters trigger is a Box component="button" height:34 (line 192) — below 44px tap target — and pagination IconButtons are width/height 28 (lines 477-478, 521-522), also sub-44. Primary touch controls.
     FIX: Bump the Filters button to height: { xs: 44, sm: 34 } and the pagination IconButtons to { xs: 40, sm: 28 } (40 acceptable for secondary paging, prefer 44) on phones via useIsMobile.

• src/features/notes/components/sidePanel/NotesContent.tsx  [component, baseDialog=na]
   - (lines 294-364 (search/filter/toggle row), 326 (minWidth:140)) Search + Filter row (lines 294-364) is a single non-wrapping flex row containing: TextField(flex:1) + creator-filter FormControl with minWidth:140 (line 326) + a 2-button ToggleButtonGroup (card/tag view). Fixed: 140 (creator select) + ~80 (two toggle buttons) = 220px + gap 1 (3 gaps ~24px) inside px:2 (32px) leaves the search TextField only ~50px on a 360px drawer — and on the 328px content area the row's fixed children (220px) plus toggle group can push past the width with no flexWrap, causing horizontal overflow.
     FIX: Restructure the row for phones: flexWrap:'wrap' on the container, OR move the creator FormControl + ToggleButtonGroup to a second row below the search on xs (search fullWidth on its own row). Reduce creator Select minWidth to { xs: 0, sm: 140 } and let it flex. Concretely: wrap search in its own full-width row and put creator-select + view-toggle in a second flex row with flexWrap.
   - (lines 460-479 (footer Add Note)) Empty-state NotesIcon fontSize:64 (line 380) and h6 text — fine width-wise. Footer Add Note button size="small" (lines 462-478) ~32px height — sub-44 tap target.
     FIX: Add Note button height: { xs: 44, sm: 'auto' } on phones. Empty-state icon is fine.

########## MINOR (one-liners) ##########
• src/features/calendar/components/trades/TradeFormDialog.tsx: Uses BaseDialog (inherits full-screen on phones) with maxWidth='md' fullWidth — correct. The inner DayHeader (DayHeader.tsx) lays its two st (+1 more)
• src/features/calendar/components/import/ColumnMapper.tsx: Each mapping row is a 4-col grid: source-column pill (1fr) + 24px arrow + target Select (1fr) + 20px tick, gap 1.25 (10px). On a 360px dialo
• src/features/calendar/components/import/AssetMappingPanel.tsx: The empty-state worked example uses a 4-col grid (value pill / count / arrow / instrument pill) with gap 0.85. At 360px the three 1fr column
• src/features/calendar/components/SelectDateDialog.tsx: Uses BaseDialog (full-screen on phones) — good, and the month grid is responsive (xs:'repeat(2,1fr)') with abbreviated month names via isSmD (+1 more)
• src/pages/PerformancePage.tsx: APP_HEADER_HEIGHT is hardcoded to 64px. On phones the fixed AppHeader is 56px (HEADER_HEIGHT_XS), so the spinner overlay height calc(100vh-6 (+1 more)
• src/features/performance/components/PerformanceCharts.tsx: Uses a local useMediaQuery(theme.breakpoints.down('sm')) named isXs instead of the shared useIsMobile() hook. Functionally identical but vio (+2 more)
• src/features/performance/components/charts/TradesListDialog.tsx: Built on BaseDialog so it inherits full-screen on phones — good, no overflow from the chrome. Inner content is mostly fluid: ResponsiveConta (+1 more)
• src/features/performance/components/charts/CumulativePnLChart.tsx: Chart width is fluid (ResponsiveContainer width='100%') so no horizontal overflow. Height is a fixed 300px on all viewports; acceptable but  (+1 more)
• src/features/performance/components/charts/DailyPnLChart.tsx: Width is fluid (no overflow). Fixed 300px height not reduced on mobile (same as Cumulative). The Max-Drawdown ReferenceLine has a right-posi
• src/features/performance/components/charts/SessionPerformanceAnalysis.tsx: Session cards grid is repeat(2,1fr) at ALL breakpoints (xs/sm/md identical). At 360px that yields ~150px-wide cards each holding 5 label:val (+1 more)
• src/features/performance/components/PerfPill.tsx: Default width is fit-content with 5 inline segments and no flexWrap and no horizontal scroll. When used by PerformanceHeader with 5 TimePeri
• src/features/performance/components/AccountStats.tsx: Two-column inset grids (1fr 1fr) at all sizes. At 360px each cell is ~150px. Values are large currency strings: Current P&L '−$7,552,661', T
• src/features/performance/components/charts/PnLChartsWrapper.tsx: The wrapper Paper uses fixed px:3 (24px each side) padding on the tab row AND the chart body at ALL breakpoints. On 360px that consumes 48px
• src/features/notes/components/NoteEditorDialog.tsx: Uses a RAW MUI <Dialog>, not BaseDialog. It DOES go full-screen below 900px (so 360px is already full-screen, content = NoteEditorBody which
• src/features/notes/components/NoteViewerDialog.tsx: Raw <Dialog>, not BaseDialog. Goes full-screen <900px so no 360px overflow (inner NoteViewerContent is responsive with px:{xs:2}). But no sa
• src/components/common/RichTextEditor.tsx: The main editor wrapper has `overflow:'hidden'` and renders the sticky EditorToolbar inside it. Because the toolbar row is nowrap and wider  (+2 more)
• src/features/notes/components/NoteViewPanel.tsx: Sticky header uses `px:4` (32px each side = 64px gutters) on a 360px screen, leaving ~296px for a date+'Edited X ago' caption (flex:1) PLUS  (+1 more)
• src/components/common/RichTextEditor/components/ImageBlock.tsx: If an embedded image entity stored an explicit numeric `width` (e.g. 600), the inner container's maxWidth becomes 600px. It's still bounded 
• src/features/events/components/EconomicEventDetailDialog.tsx: Built on BaseDialog so it is already full-screen on phones with safe-area insets. No 'make full-screen' needed. Inner content is mostly resp (+2 more)
• src/components/notifications/NotificationsPopover.tsx: Popover width is already responsive: `isNarrow ? calc(100vw - theme.spacing(2)) : 380` with `maxWidth: '100vw'`, anchored bottom/right. At 3
• src/features/events/components/EconomicEventRow.tsx: Row inner grid is `gridTemplateColumns: '52px 1fr auto'` with gap:1.5 (12px). 52 + 12 + 12 = 76px of fixed/gap chrome, leaving ~252px for th (+1 more)
• src/features/events/components/EconomicEventsView.tsx: This shared view already has a mature mobile story: DayRail row-variant that compresses 7 days to flex:1 (no fixed minWidth, fits 328px), Cu
• src/features/events/components/EconomicEventDetailPanel.tsx: Stats grid is `gridTemplateColumns: { xs: '1fr 1fr', sm: 'repeat(5, 1fr)' }` — 2-up on xs, good. This panel renders inside the lg+ SidePanel
• src/features/events/components/EconomicEventsPanel.tsx: Outer PANEL_WIDTH is `{ xs: '100%', sm: 'min(45%, 380px)' }` (good — full width on phones) but INNER_WIDTH is `{ xs: '100%', sm: 'min(45vw, 
• src/components/notifications/NotificationRow.tsx: Row grid is `gridTemplateColumns: '12px 1fr 80px'` with columnGap:1.5 (12px each gap) + px:2 (16px each side). Fixed chrome = 16+12+12+12+80
• src/features/orion/components/aiChat/AIChatInterface.tsx: Three children in a non-wrapping flex row: Draft.js mention input (flex:1, minWidth:0 — good), 32x32 image button, and Send button (px:1.5,  (+3 more)
• src/features/orion/components/aiChat/HtmlMessageRenderer.tsx: Tables are width:100% with no overflow wrapper here (unlike MarkdownRenderer). A briefing table with many columns or long unbreakable cell t
• src/features/orion/components/aiChat/TradeCard.tsx: The 3-zone footer (R:R left · date center · images/partials/session right) packs up to 3 meta items in the right zone with gap:1. On a 328px
• src/features/orion/components/aiChat/ReminderListItem.tsx: Layout is sound (icon + flex:1 minWidth:0 truncating text column + cancel button) so no overflow. The only concern is the cancel IconButton 
• src/features/orion/components/aiChat/ReminderBatchListItem.tsx: Same pattern as ReminderListItem (icon + flex:1 minWidth:0 + cancel button), no overflow. Cancel IconButton tap target < 44px; the title + '
• src/features/orion/components/aiChat/OrionPanel.tsx: Both width tiers use xs:'100%' so the slide-in focus panel fills the phone width — good, no overflow. The pager uses a width:'200%' translat
• src/features/orion/components/aiChat/CitationsSection.tsx: Compact citations list has maxWidth:350 inside a Popover with anchorOrigin bottom-left / no overflow boundary. At 360px the 350px panel bare
• src/features/orion/components/aiChat/ToolUsageChip.tsx: Popover variant uses maxWidth:380 anchored bottom-left with no viewport boundary; at 360px a 380px popover overflows the right edge. The inl
• src/features/orion/components/aiChat/MemoryLogsPanel.tsx: Tabs are fullWidth (good). The expanded audit Before/After blocks render row.before_text/after_text in a monospace Typography with whiteSpac
• src/features/orion/components/settings/CustomToolCard.tsx: The header right slot packs a Switch (~40px) plus three IconButtons (size small ~34px each) = ~140px, beside a wrapping title (name + read-o
• src/features/orion/components/settings/CustomToolFormPanel.tsx: Panel padding is already xs-aware. The footer is Back + primary action with justify space-between — fits. The real risk is the embedded Step
• src/features/orion/components/settings/ReviewStep.tsx: Two monospace multiline TextFields render JSON schema. Long unbroken JSON lines (e.g. a deep nested schema) won't wrap inside a textarea — t (+1 more)
• src/features/orion/components/settings/StageGates.tsx: 'Run audit' + 'Test-fire webhook' in a non-wrapping row. 'Test-fire webhook' is a long label; the two buttons together can exceed 328px, for
• src/features/orion/components/settings/SecretRevealBox.tsx: The TextField is fullWidth (flex shrinks) so the row won't overflow, but the masked 64-dot / 64-char-hex value is monospace 12px and on reve
• src/features/orion/components/settings/WebhookDocsAccordion.tsx: Four <pre> code snippets use whiteSpace:'pre' (no wrap) with overflow:'auto'. Long lines like the crypto.timingSafeEqual(...) call exceed 32
• src/features/orion/components/orionTasks/OrionTasksContent.tsx: The status strip left side packs 'MARKET RESEARCH' mono label + status pill + 'updated Xm ago' + an optional 'N runs failed' warning, and th (+1 more)
• src/features/orion/components/orionTasks/TaskResultCard.tsx: Header is well-structured (left cluster has minWidth:0 + ellipsis on the task-type label, right cluster flexShrink:0 with time + 3 small Ico
• src/components/landing/LandingHero.tsx: Display headline fontSize xs:'2.6rem' (~41.6px) with letterSpacing:-0.04em. 'that finds your edge.' is the longest line — 'finds' / 'edge' w (+1 more)
• src/components/landing/sections/EventsSection.tsx: Each event row is gridTemplateColumns:'64px 1fr auto' with gap:1.75 (14px) and px:2.25 (18px) on a card that goes full-width at xs (single-c
• src/components/landing/sections/PerformanceSection.tsx: Stats row is gridTemplateColumns:'repeat(3, 1fr)' at ALL breakpoints — never stacks. At ~328px inner that is ~3 columns of ~105px with gap:{
• src/components/landing/sections/OrionContextSection.tsx: Demo grid stacks at xs (gridTemplateColumns:{xs:'1fr'}). The synthesis answer card overlays absolutely-positioned typed text on a hidden spa
• src/pages/PricingPage.tsx: No custom h2 is defined in theme.ts (only h3-h6 are overridden), so MUI default h2 = 3.75rem (60px). Single word 'Pricing' fits at 360px but (+2 more)
• src/features/billing/pricing/ComparisonTable.tsx: 5-column MUI <Table> (Feature + Free/Lite/Pro/Elite). It is already wrapped in Box overflowX:'auto' (line 80) so it horizontally scrolls ins
• src/pages/legal/LegalPage.tsx: Hardcodes the 64px desktop header height. On phones the fixed AppHeader is 56px (HEADER_HEIGHT_XS), so this self-scrolling column is 8px too (+1 more)
• src/pages/SharedCalendarPage.tsx: The error/loading/empty states are responsive (Container maxWidth='md' px:{xs:1,sm:3}), but the success branch renders TradeCalendar directl (+1 more)
• src/pages/SharedNotePage.tsx: Tag row already wraps (flexWrap='wrap') and chips are size='small' — good. Title (line 220) correctly uses wordBreak:'break-word'. The remai (+1 more)
• src/pages/AccountBillingPage.tsx: p:4 = 32px padding each side inside a Container maxWidth='md'. At 360px (Container gutters + 32px*2) the card body is ~264px wide — tight fo (+2 more)
• src/components/auth/SupabaseAuthTest.tsx: Renders a raw UUID (and a token substring at line 158) inside body2 with no wordBreak. A 36-char UUID is one unbreakable token-ish string th
• src/features/billing/components/CalendarLimitDialog.tsx: RAW MUI <Dialog>, not BaseDialog, and unlike the other dialogs here it uses the stock DialogTitle/Content/Actions primitives (not the app's 
• src/components/sidePanel/SidePanelHeader.tsx: Lines 36-43 and 65-77: back IconButton and close IconButton use size="small" with fontSize="small" icons. size="small" IconButton is ~30px a
• src/components/common/UnifiedDrawer.tsx: Verified default width={ xs: '100%', sm: 450 } (line 54) and full-height column layout (line 96) — good. BUT there is no safe-area inset han (+1 more)
• src/features/calendar/components/sidePanel/CalendarsListContent.tsx: WatchRow (lines 686-815) packs HeroSwatch(28) + name/meta(flex) + Sparkline(width 44) + PnL box(minWidth 78) + actions IconButton into a sin (+1 more)
• src/features/calendar/components/sidePanel/DayTradesContent.tsx: Footer (lines 221-264): up to three buttons (Events, Gallery, Add Trade) in a single flex row with justifyContent:'flex-end' and gap 1, NO f (+1 more)
• src/features/calendar/components/sidePanel/TagManagementContent.tsx: Search + filter row (lines 675-721): TextField (fullWidth) + FormControl with minWidth:130 (line 696) side by side in a flex row with gap 0. (+2 more)
• src/features/orion/components/sidePanel/AIChatContent.tsx: History overlay conversation rows (lines 755-904): ListItemButton is a flex row with the text column (flex:1, minWidth:0 — good, ellipsizes) (+1 more)
